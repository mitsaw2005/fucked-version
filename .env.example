# SpareAI — Environment Configuration
# Copy this file to .env and adjust as needed

# Path to the local Excel data file (relative to project root)
DATA_PATH=data/data.xlsx

# Optional: Disable local auth so FastAPI /docs works without login
DISABLE_AUTH=True
